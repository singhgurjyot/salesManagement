import java.awt.*;
import javax.swing.*;

class AddEdit
{
	AddEdit()
	{
		JFrame f = new JFrame();
		f.setSize(1400,1000);
		f.setLocationRelativeTo(null);
		f.setDefaultCloseOperation(JFrame.EXIT_ON_CLOSE);
		f.setVisible(true);

	}
}